#ifndef CCOUNTER_COUNTF_H
#define CCOUNTER_COUNTF_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int countChars(FILE *f);
int countWords(FILE *f);
int countLines(FILE *f);

#endif //CCOUNTER_COUNTF_H
